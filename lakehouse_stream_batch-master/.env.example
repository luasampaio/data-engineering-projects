# Exemplo de configuração de variáveis de ambiente para o projeto Lakehouse Stream & Batch
# Copie este arquivo para .env e preencha os valores conforme seu ambiente

# Kafka
KAFKA_BOOTSTRAP=kafka:9092

# Diretório para arquivos em lote (parquet)
BATCH_DIR=/data/batch

# Taxa de geração de ordens por segundo
ORDER_RATE_PER_SEC=100

# Controle do auto-start do spammer (1 para ativar automaticamente, 0 para não)
SPAMMER_AUTO_START=1

# Configurações do Postgres
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=demo
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin

# (Opcional) Outras variáveis customizadas podem ser adicionadas abaixo
